# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is Team 9's Agentic AI Hackathon project. The repo is in early development — the `Backend` branch is the active development branch, with `main` as the integration target.

## Participant Resources

The `participant-resources/` directory contains reference material for the hackathon (HTML slide decks and PDFs). These are read-only reference files, not part of the application.

## Repository Structure

As development progresses, update this file with:
- Build, lint, and test commands
- How to run the dev server / individual services
- High-level architecture (e.g., how the backend and any frontend/agent components interact)
- Environment variable requirements (`.env` structure, required keys)
